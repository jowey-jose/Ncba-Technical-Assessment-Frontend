// This page shows a List of Services, Users need to have logged in Otherwise won't see any services listed.

import React from "react";


const LandingPage = () => {
    
    return (
        <div className="container">
            <header className="jumbotron">
                <h3>Landing Page: NCBA FrontEnd Assessment</h3>
                {/* <strong>Services List:</strong>
                <ul>
                    { content.map((service, index) => <li key={index}>{service}</li>)}
                </ul> */}
            </header>
        </div>
    );
};
export default LandingPage;